name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  
jobs:

  build:

    runs-on: dos-box-runner
    steps:
    - uses: actions/checkout@v3
    - name: List the Docker image
      run: docker images
